theme = "white_and_black"

[keys.normal]
# Simplifies exiting. Collapes all exits on the 'esc' key
esc = ["collapse_selection", "keep_primary_selection"]
# Move faster around by using Alt-hjkl
A-l = "move_next_word_start"
A-h = "move_prev_word_start"
A-j = "goto_next_paragraph"
A-k = "goto_prev_paragraph"
# Jump straight to a word faster
";" = "goto_word"
# Find some operation for ','
"," = "no_op"
# Move between buffer more efficiently
"}" = "goto_next_buffer"
"{" = "goto_previous_buffer"

[keys.normal.z]
# Faster scrolling through zj/zk
j = "page_cursor_half_down"
k = "page_cursor_half_up"

[editor]
bufferline = "multiple"
# Not sure if I really need this. But we will see.
#line-number = "relative"

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"
